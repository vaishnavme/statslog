{
  "name": "server",
  "version": "0.1.0",
  "description": "backend for statslog",
  "keywords": [
    "analytics",
    "stats",
    "logs"
  ],
  "license": "MIT",
  "author": "Vaishnav Chandurkar",
  "main": "dist/index.js",
  "scripts": {
    "dev": "nodemon --watch src --ext ts --exec ts-node src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^22.19.3",
    "nodemon": "^3.1.11",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
